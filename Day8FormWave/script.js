document.addEventListener('DOMContentLoaded', init);

function init() {
//    const labels = document.querySelectorAll('.form-control label')
//    labels.forEach(function(label){
//     label.innerHTML = label.innerText
//     .split('')
//     .map((x, idx) => `<span>${x}</span>`)
//     .join('')
//    })
}